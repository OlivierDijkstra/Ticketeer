{
    "name": "api",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "postinstall": "composer install",
        "clean": "php artisan optimize:clear && rm -rf vendor node_modules storage/app/public/*",
        "dev": "vite dev",
        "serve": "php artisan serve",
        "lint": "./vendor/bin/pint",
        "test": "./vendor/bin/phpunit",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}